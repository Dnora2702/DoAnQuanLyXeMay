<Window x:Class="QLXeMay.Views.Auth.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QLXeMay.ViewModels.Auth"
        xmlns:helper="clr-namespace:QLXeMay.Helpers"
        Name="loginWindow"
        mc:Ignorable="d"
        Title="Đăng Nhập" Height="580" Width="430"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <!-- Outer gradient wrapper -->
    <Border CornerRadius="20">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1E3A8A" Offset="0"/>
                <GradientStop Color="#3B82F6" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="6"
                              Color="#000000" Opacity="0.35"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top branding strip -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,28,0,0">
                <Border Background="#FFFFFF22" CornerRadius="14"
                        Width="60" Height="60" HorizontalAlignment="Center">
                    <Image Source="/Images/Icons/motorcycle.png" Height="36"/>
                </Border>
                <TextBlock Text="XE MÁY PRO" Foreground="White"
                           FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,10,0,2"/>
                <TextBlock Text="Hệ thống quản lý chuyên nghiệp"
                           Foreground="#BFDBFE" FontSize="12"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>
            </StackPanel>

            <!-- White card -->
            <Border Grid.Row="1" CornerRadius="20" Background="White"
                    Margin="20,0,20,20">
                <StackPanel Margin="32,28">

                    <TextBlock Text="Chào mừng trở lại 👋"
                               FontSize="18" FontWeight="Bold"
                               Foreground="#1E293B" Margin="0,0,0,4"/>
                    <TextBlock Text="Vui lòng đăng nhập để tiếp tục"
                               FontSize="13" Foreground="#94A3B8"
                               Margin="0,0,0,24"/>

                    <!-- Tên đăng nhập -->
                    <TextBlock Text="TÊN ĐĂNG NHẬP" Style="{StaticResource FieldLabel}"/>
                    <Border Style="{StaticResource InputBorder}">
                        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource PlainTextBox}"/>
                    </Border>

                    <!-- Mật khẩu -->
                    <TextBlock Text="MẬT KHẨU" Style="{StaticResource FieldLabel}"/>
                    <Border Style="{StaticResource InputBorder}">
                        <PasswordBox x:Name="txtPassword"
                                     Style="{StaticResource PlainPasswordBox}"
                                     helper:PasswordBoxHelper.Attach="True"
                                     helper:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Border>

                    <!-- Lỗi -->
                    <Border Background="#FEE2E2" CornerRadius="8" Padding="12,10"
                            Margin="0,0,0,14"
                            Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#DC2626" FontSize="13"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <!-- Nút đăng nhập -->
                    <Button Content="ĐĂNG NHẬP" Height="46" Cursor="Hand"
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource PrimaryButton}"
                            FontSize="14" FontWeight="Bold"/>

                    <!-- Link đăng ký -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center" Margin="0,16,0,0">
                        <TextBlock Text="Chưa có tài khoản?"
                                   Foreground="#94A3B8" FontSize="13"
                                   VerticalAlignment="Center"/>
                        <Button Content=" Đăng ký ngay"
                                Foreground="#2563EB" FontSize="13"
                                FontWeight="SemiBold" Background="Transparent"
                                BorderThickness="0" Cursor="Hand" Padding="4,0"
                                Command="{Binding RegisterWindowCommand}"/>
                    </StackPanel>

                    <!-- Thoát -->
                    <Button Content="Thoát ứng dụng"
                            Margin="0,8,0,0" Height="36"
                            Background="Transparent" BorderThickness="0"
                            Foreground="#CBD5E1" Cursor="Hand"
                            FontSize="12"
                            Click="btnExit_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
